{
  "name": "preact-pages",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-maze":"npm ci --prefix src/demo/maze-generation-visualization/; npm run build --prefix src/demo/maze-generation-visualization/; mkdir -p public/demo/maze-generation-visualization; mv -f src/demo/maze-generation-visualization/build/* public/demo/maze-generation-visualization",
    "build-submodule":"npm run build-maze",
    "build-main":"tsc && vite build",
    "build": "npm run build-submodule; npm run build-main",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "sass": "^1.64.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
